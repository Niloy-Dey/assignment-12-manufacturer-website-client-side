import React from 'react';

const AllOrders = ({order}) => {
    console.log(order);
    return (
        <div className='m-44'>
            <h1>{order.Name} </h1>
            <h1>{order.Name} </h1>
            <h1>{order.Name} </h1>
            <h1>{order.Name} </h1>
            <h1>{order.Name} </h1>
            <h1>{order.Name} </h1>
            <h1>{order.Name} </h1>
            <h1>{order.Name} </h1>
            <h1>{order.Name} </h1>
            
        </div>
    );
};

export default AllOrders;